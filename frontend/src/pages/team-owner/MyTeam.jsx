import YourLockerRoom from './YourLockerRoom';
/* ignore-breakpoints */

// Backward-compatible wrapper for tests and legacy imports.
export default function MyTeam(props) {
  return <YourLockerRoom {...props} />;
}
